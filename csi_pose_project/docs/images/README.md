# Images

This folder stores diagrams, screenshots, and figures used in the README.

Current files:

- `system_pipeline.svg`: included system pipeline diagram.
- `tool_csi_wave.jpg`: CSI amplitude comparison with and without a person.

Recommended files to add later:

- `mediapipe_label.png`: MediaPipe keypoint label example.
- `esp32_csi_setup.jpg`: ESP32 CSI data collection setup.
- `prediction_result.png`: predicted keypoints vs ground truth.
- `training_curve.png`: model training curve.

See `IMAGE_GUIDE.md` for suggested captions and usage notes.
